#include "main.h"
#include <stdio.h>
/**
 * main - print program name
 * @argc: nbr of arguments
 * @argv: arra of all the arguments
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
